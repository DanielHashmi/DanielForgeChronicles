import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'j72oknuv',
    dataset: 'production',
  },
  studioHost: 'danielforgechronicles',
  autoUpdates: true,
})
